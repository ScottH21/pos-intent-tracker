# Raw query results (regenerated by refresh.sh)
data/leads_raw.json
data/accounts_raw.json
data/raw_*.json
data/raw/

# Keep accounts.json (the app needs it)
!data/accounts.json

# OS files
.DS_Store
